node_modules/
build/
dist/
coverage/
.DS_Store
*.log
# Runtime-generated persistent store (local fallback engine). Never commit live data.
backend/data/store.json
# Runtime-uploaded media files.
backend/data/uploads/
!backend/data/uploads/.gitkeep
# Environment files (real secrets). Template is committed instead.
.env*
!.env.example
# Windows reserved-device artifact that can be created accidentally by a shell
# redirect (e.g. `... > nul`). Never a real project file; keep it out of git.
nul
